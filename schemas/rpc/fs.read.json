{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "fs.read parameters",
  "type": "object",
  "additionalProperties": false,
  "required": ["path"],
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "description": "Path relative to the sandbox root that should be read."
    }
  }
}
